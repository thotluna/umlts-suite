/**
 * Shared measurement constants for UML Elements.
 * Centralized to ensure model and layout stay in sync.
 */
export const MEASURE_CONFIG = {
  CHAR_WIDTH: 7.2, // Balanced for proportional sans-serif
  LINE_HEIGHT: 28,
  HEADER_HEIGHT_NORMAL: 45,
  PADDING_BOTTOM: 15,
  PADDING_X: 40, // 20px each side for perfect symmetry
  MIN_WIDTH: 120,
  STEREOTYPE_HEIGHT: 14,
  SECTION_DIVIDER_HEIGHT: 8,
} as const
